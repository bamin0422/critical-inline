{
  "name": "critical-inline-monorepo",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "tsc -p packages/core/tsconfig.json --noEmit && tsc -p packages/unplugin/tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@changesets/cli": "2.27.9",
    "tsup": "8.3.5",
    "typescript": "5.5.3",
    "vitest": "2.1.8"
  }
}
