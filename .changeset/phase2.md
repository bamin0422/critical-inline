---
'unplugin-critical-inline': minor
---

Phase 2: config manifest `entries` option, automatic `<head>` injection for Vite (`transformIndexHtml`) and webpack (`html-webpack-plugin`, optional peer), plus `?critical` import-transform integration tests for webpack and Rollup. `critical-inline` core is unchanged in this release.
